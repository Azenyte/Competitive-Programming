#include<bits/stdc++.h>
using namespace std;

// House has only one socket 
// Extend it with power strips, each with A sockets into B or more 
// 1 strips with A sockets
// B = A + more
// print the minimum number of power strips required? 
// I don't know if this is bad translation or am I dumb? probably both. 
// I don't get the problem  
// 4 sockets | 10 empty sockets

int main(){
	int a, b;
	cin >> a >> b;
	
