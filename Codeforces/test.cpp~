#include <bits/stdc++.h> 
using namespace std; 

void solve(){
	int n,q,x,j; 
	cin >> n;
	vector<int> p(n);
	for(int i=0; i<n; i++){
		cin >> p[i];
	}
	sort(p.begin(),p.end());
	cin >> q;
	while(q--){
		cin >> x;
		for(auto i: p){
			if(x > i){
				j++;
			}
		}

	}
	

	



}

int main(){
	solve();
}
